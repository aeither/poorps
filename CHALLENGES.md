
- using project.yaml or config.staging.json is confusing
- My contract needs to implement IReceiver
- how to test IReceiver implemented contracts with foundry (long iterations on testnet)
- need to use secrests. cant run `cre secrets list --target staging-settings` because need deploy and currently cant `workflow deployment is currently in early access. We're onboarding organizations gradually`
- ai assistant confuses go with ts syntax. getting secret 
